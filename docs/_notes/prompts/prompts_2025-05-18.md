src/js/view/TrestleView.js is a working version. A new version src/js/view/TrestleView-extra.js was created including new components. This version broke the core functionality.
We are looking at the new features and introducing them to the working TrestleView.js one at a time, so I can test at every step. Now we need to do the Breadcrumb view. This caused a breakage last time, throwing things into an infinite loop. Watch out this time.

