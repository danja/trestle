Ignore the docs dir, it contains notes from previous work. Analyse the codebase and determine how the components visible in the toolbar at the top of the html may be implemented following the existing patterns and best practices. Create an itemised list of the components and their functionality together with a plan to build them. Save this in plan.md in the root dir for now.

Ignore the contents of the docs dir. Read plan.md and analyze existing to implement in whatever order you think best. For each component implementation we will need to create a new test file in the test dir. Use the existing tests as templates. We need to make sure each individual component is tested before moving onto the next. Follow existing patterns and best practices using modern ES. 