import { describe, it, expect } from 'vitest';
import ExpanderButton from '../src/js/view/components/ExpanderButton.js';

describe('ExpanderButton', () => {
  it('should be defined', () => {
    expect(ExpanderButton).toBeDefined();
  });
}); 