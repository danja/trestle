import { describe, it, expect } from 'vitest';
import ContextMenu from '../src/js/view/components/ContextMenu.js';

describe('ContextMenu', () => {
  it('should be defined', () => {
    expect(ContextMenu).toBeDefined();
  });
}); 