import { describe, it, expect } from 'vitest';
import InlineEditor from '../src/js/view/components/InlineEditor.js';

describe('InlineEditor', () => {
  it('should be defined', () => {
    expect(InlineEditor).toBeDefined();
  });
}); 